/** @type {import('next').NextConfig} */
const nextConfig = {
    /* config options here */
    experimental: {
        viewTransition: true,
    },
    reactCompiler: true,
};

export default nextConfig;
